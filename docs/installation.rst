============
Installation
============

At the command line::

    $ easy_install tagging-app

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tagging-app
    $ pip install tagging-app
